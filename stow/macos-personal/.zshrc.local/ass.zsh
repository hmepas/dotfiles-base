# short for assistant, to ask assistant and get results back to stdout
# call like ass "tell me a programmers joke"
alias ass='cursor-agent -p --model composer-1'

# same but for gemini
alias gask='gemini --model gemini-2.5-flash-lite'
